{
  "name": "docq",
  "version": "1.0.0",
  "private": true,
  "description": "Chat with your documents using AI-powered retrieval",
  "workspaces": [
    "packages/shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w packages/shared && npm run build -w server && npm run build -w client",
    "lint": "npm run lint -w server && npm run lint -w client",
    "test": "npm run test -w server",
    "test:e2e": "npm run test:e2e -w client",
    "db:seed": "npm run db:seed -w server",
    "clean": "rimraf node_modules packages/*/node_modules server/node_modules client/node_modules"
  },
  "devDependencies": {
    "concurrently": "^9.2.4",
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
